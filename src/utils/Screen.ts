import { Dimensions } from "react-native";

const { width: SCREEN_WIDTH, height: SCREEN_HEIGHT } = Dimensions.get("window");

const CONTAINERHP = 20;
export { SCREEN_WIDTH, SCREEN_HEIGHT, CONTAINERHP };
